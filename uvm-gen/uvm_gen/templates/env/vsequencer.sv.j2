// -----------------------------------------------------------------------------
// {{ ip }}_vsequencer.sv - virtual sequencer
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// -----------------------------------------------------------------------------
class {{ ip }}_vsequencer extends uvm_sequencer;

  `uvm_component_utils({{ ip }}_vsequencer)

  {{ ip }}_env_cfg cfg;

  // Handles to the per-interface sequencers, assigned by {{ ip }}_env.
  // A handle is NULL when its agent is passive (e.g. SoC integration) -
  // virtual sequences must check before starting stimulus.
{% for a in agents %}
  {{ ip }}_{{ a.name }}_sequencer {{ a.name }}_sqr;
{% endfor %}
{% for v in vips %}
  uvm_sequencer_base {{ v.name }}_sqr;  // Cadence {{ v.protocol|upper }} VIP '{{ v.name }}' ({{ v.role }})
{% endfor %}

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

endclass : {{ ip }}_vsequencer
