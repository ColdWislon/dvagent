# {{ ip }} — Verification Context

[Pre-filled by uvm-gen v{{ tool_version }} from the generated architecture; keep it
under ~100 lines and CURRENT — agents read this before any work on the IP.
Brackets mark what only a human can fill.]

## What this block does
[2–4 sentences: function, key interfaces, clock/reset domains. Link the
spec: docs/spec/<file> §relevant. If the spec is not agent-readable
in-repo, say where humans find it — agents must then ask instead of
guessing.]

## Testbench architecture (10 lines max)
- Env: `{{ ip }}_env` — agents: [{% for a in agents %}{{ ip }}_{{ a.name }}_agent ({{ a.mode|upper }}){% if not loop.last %}, {% endif %}{% endfor %}]{% if vips %},
  Cadence VIPs: [{% for v in vips %}{{ v.name }} ({{ v.protocol|upper }}, {{ v.role }}, placeholder until enabled){% if not loop.last %}, {% endif %}{% endfor %}]{% endif %}

- Scoreboard: `{{ ip }}_scoreboard` (compare logic: PLACEHOLDER-CHECK stubs, reference model: [none yet])
- Coverage: {% for a in agents %}`{{ ip }}_{{ a.name }}_cov`{% if not loop.last %}, {% endif %}{% endfor %}{% if not agents %}—{% endif %} (example bins — refine + tag `// VP-xxx`)
- Virtual sequencer: `{{ ip }}_vsequencer` — handles: [{% for a in agents %}{{ a.name }}_sqr{% if not loop.last %}, {% endif %}{% endfor %}{% for v in vips %}, {{ v.name }}_sqr{% endfor %}] (null when passive)
- RAL: yes — `{{ ip }}_reg_block` stub + `{{ ip }}_reg_adapter` (replace with ralgen/IP-XACT model)
- Top harness: `tb_top` in `tb/tb_top.sv` (DUT `{{ dut_module }}`, stubbed via `sim/dut.f`)
- Base test: `{{ ip }}_base_test`; smoke test: `{{ ip }}_smoke_test` (passes on the fresh env)
- chkq base: `{{ ip }}_chkq_base_test` (reparented chkq_base_test): not yet — kit staged in `dv/tests/negative/`

## Clock / reset / CDC inventory
[tb_top generates one clock (CLK_PERIOD=10ns) and one async-low reset for all
interfaces — replace with the real domain list:
- Clock domains: [name: range/ratio constraints, ...]
- Reset domains: [name: async/sync, soft-reset behavior, ...]
- Crossings: [src→dst: scheme (2FF / gray / handshake / async FIFO), ...]
- CDC report location: [path / tool run reference]]

## Current verification focus
[Freshly generated environment. Suggested order: wire real protocol signals
in the interfaces/drivers/monitors (TODO markers), connect the DUT in
tb_top + flip sim/dut.f, then author real checks from the PLACEHOLDER-CHECK
inventory. What should an agent NOT touch right now?]

## Known quirks and traps
[The list that saves hours: protocol deviations from standard, known
waived warnings, flaky areas, sequences with non-obvious preconditions,
signals with misleading names. One line each.]

## Check ID inventory
[Stable uvm_error IDs this env's checkers emit, one line each — none exist
yet; the scoreboard stubs are PLACEHOLDER-CHECK. Keep in sync with
dv/lists/chkq.list once real checks land, e.g.:
`SCBD_DATA_CMP` — read-data compare against model]

## Commands
- Compile: `make -C sim compile`   Smoke: `make -C sim run TEST={{ ip }}_smoke_test`
- Configuration select: `CFG=../cfg/<config>.yaml`   All-passive: `PASSIVE=1`
- Sanity list: `dv/lists/sanity.list`   chkq list: `dv/lists/chkq.list`
- Full mapping of the `dv` golden commands onto this flow:
  `.github/instructions/uvm-gen-env.instructions.md`
- Params of this configuration ({{ config_name }}): {% for p in params %}`{{ p.name }}={{ p.value }}`{% if not loop.last %}, {% endif %}{% endfor %}{% if not params %}—{% endif %}

## Ownership
Block owner: [name]. Checker sign-off: [name]. Escalate RTL-suspect
reports to: [name/channel].
