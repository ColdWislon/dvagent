# dv/status/ - machine-readable session sidecars land here (generated by
# agent sessions per the evidence contract in .github/copilot-instructions.md:
# session_<date>.json plus review.json / lint.json / triage.json). The
# verif-cockpit skill renders pending human decisions from these files.
