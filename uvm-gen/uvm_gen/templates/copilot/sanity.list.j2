# Sanity regression list — {{ ip }}
# Purpose: fast ripple-safety net (target < 15 min wall). Run mandatory
# after any change to existing sequence items, base constraints, or
# shared sequences (stim-writer / coverage-closer rule).
# This flow: one line per run, executed as
#   make -C sim run TEST=<test> SEED=<seed> [PLUSARGS='<plusargs>']
# (SEED=random lets xrun pick; cfg_tool records the chosen seed.)
# Format: <test> <seed|random> [plusargs]
{{ ip }}_smoke_test          random
{{ ip }}_smoke_test          1
# [add 3-6 tests spanning the main traffic types and one corner]
