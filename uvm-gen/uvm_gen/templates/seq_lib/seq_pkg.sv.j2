// -----------------------------------------------------------------------------
// {{ ip }}_seq_pkg.sv - virtual sequence library for {{ ip }}
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// -----------------------------------------------------------------------------
package {{ ip }}_seq_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

{% for a in agents %}
  import {{ ip }}_{{ a.name }}_agent_pkg::*;
{% endfor %}
  import {{ ip }}_env_pkg::*;

  `include "{{ ip }}_base_vseq.sv"
  `include "{{ ip }}_smoke_vseq.sv"

endpackage : {{ ip }}_seq_pkg
