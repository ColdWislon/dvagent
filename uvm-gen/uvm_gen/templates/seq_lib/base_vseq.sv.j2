// -----------------------------------------------------------------------------
// {{ ip }}_base_vseq.sv - base virtual sequence
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// -----------------------------------------------------------------------------
class {{ ip }}_base_vseq extends uvm_sequence;

  `uvm_object_utils({{ ip }}_base_vseq)
  `uvm_declare_p_sequencer({{ ip }}_vsequencer)

  function new(string name = "{{ ip }}_base_vseq");
    super.new(name);
  endfunction

  // The env configuration as seen from the virtual sequencer.
  virtual function {{ ip }}_env_cfg get_cfg();
    return p_sequencer.cfg;
  endfunction

endclass : {{ ip }}_base_vseq
