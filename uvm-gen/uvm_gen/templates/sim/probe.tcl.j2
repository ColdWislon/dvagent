# probe.tcl - SHM waveform probing for 'make waves' (generated by uvm-gen v{{ tool_version }})
database -open waves -into waves.shm -default
probe -create tb_top -all -depth all -database waves
run
exit
