# -----------------------------------------------------------------------------
# dut.f - design/RTL filelist for {{ ip }} (own -f domain, never merged with DV)
# Generated by uvm-gen v{{ tool_version }}. Never overwritten on re-run.
# -----------------------------------------------------------------------------
# Out of the box the generated stub keeps 'make compile' and the smoke test
# green (the DUT ports are not wired yet - see the TODO in tb/tb_top.sv):
../tb/{{ dut_module }}_stub.sv

# TODO: once the DUT ports are connected in tb_top.sv, switch to the real RTL:
# comment the stub line above and uncomment the filelist below (from the
# configuration YAML's dut.rtl_filelist{% if not rtl_filelist %} - none was given at generation time{% endif %}).
# The RTL filelist may itself use -f includes.
{% if rtl_filelist_sim %}
# -f {{ rtl_filelist_sim }}
{% else %}
# -f ../../rtl/{{ ip }}.f
{% endif %}
