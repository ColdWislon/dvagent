// -----------------------------------------------------------------------------
// {{ ip }}_{{ a.name }}_cfg.sv - configuration object for the '{{ a.name }}' agent
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// -----------------------------------------------------------------------------
class {{ ip }}_{{ a.name }}_cfg extends uvm_object;

  // Active/passive switch. At SoC level flip this to UVM_PASSIVE from
  // {{ ip }}_env_cfg (e.g. set_all_passive()): the RTL then drives the bus
  // while monitor, scoreboard and coverage stay live. Driver and sequencer
  // are not constructed when passive - no code edits needed.
  uvm_active_passive_enum is_active = {{ a.uvm_active }};

  bit has_coverage = 1;
  bit has_checks   = 1;

  // Virtual interface. Always assigned by the testbench/integrator through
  // this config object - components never fetch it by hierarchical path,
  // which keeps the env droppable into any SoC environment.
  virtual {{ ip }}_{{ a.name }}_if vif;

  `uvm_object_utils_begin({{ ip }}_{{ a.name }}_cfg)
    `uvm_field_enum(uvm_active_passive_enum, is_active, UVM_ALL_ON)
    `uvm_field_int(has_coverage, UVM_ALL_ON)
    `uvm_field_int(has_checks,   UVM_ALL_ON)
  `uvm_object_utils_end

  function new(string name = "{{ ip }}_{{ a.name }}_cfg");
    super.new(name);
  endfunction

endclass : {{ ip }}_{{ a.name }}_cfg
