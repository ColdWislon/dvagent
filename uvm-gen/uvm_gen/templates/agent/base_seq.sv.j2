// -----------------------------------------------------------------------------
// {{ ip }}_{{ a.name }}_base_seq.sv - base sequence for the '{{ a.name }}' agent
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// -----------------------------------------------------------------------------
class {{ ip }}_{{ a.name }}_base_seq extends uvm_sequence #({{ ip }}_{{ a.name }}_seq_item);

  `uvm_object_utils({{ ip }}_{{ a.name }}_base_seq)

  rand int unsigned num_items = 4;

  constraint c_num_items { num_items inside {[1:16]}; }

  function new(string name = "{{ ip }}_{{ a.name }}_base_seq");
    super.new(name);
  endfunction

  virtual task body();
    repeat (num_items) begin
      req = {{ ip }}_{{ a.name }}_seq_item::type_id::create("req");
      start_item(req);
      // TODO: layer '{{ a.name }}'-specific constraints or derived sequences
      // on top of this base randomization.
      if (!req.randomize())
        `uvm_error("RNDFAIL", "{{ ip }}_{{ a.name }}_seq_item randomization failed")
      finish_item(req);
    end
  endtask

endclass : {{ ip }}_{{ a.name }}_base_seq
