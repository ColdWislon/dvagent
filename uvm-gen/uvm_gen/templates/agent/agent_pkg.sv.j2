// -----------------------------------------------------------------------------
// {{ ip }}_{{ a.name }}_agent_pkg.sv - package for the '{{ a.name }}' agent
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// Self-contained: no references to tb_top or absolute hierarchy anywhere in
// this package (SoC-reusable by construction).
// -----------------------------------------------------------------------------
package {{ ip }}_{{ a.name }}_agent_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

  `include "{{ ip }}_{{ a.name }}_seq_item.sv"
  `include "{{ ip }}_{{ a.name }}_cfg.sv"
  `include "{{ ip }}_{{ a.name }}_sequencer.sv"
  `include "{{ ip }}_{{ a.name }}_driver.sv"
  `include "{{ ip }}_{{ a.name }}_monitor.sv"
  `include "{{ ip }}_{{ a.name }}_agent.sv"
  `include "{{ ip }}_{{ a.name }}_base_seq.sv"

endpackage : {{ ip }}_{{ a.name }}_agent_pkg
