// -----------------------------------------------------------------------------
// {{ ip }}_{{ a.name }}_sequencer.sv - sequencer for the '{{ a.name }}' agent
// Generated by uvm-gen v{{ tool_version }} (IP: {{ ip }}). Never overwritten on re-run.
// -----------------------------------------------------------------------------
class {{ ip }}_{{ a.name }}_sequencer extends uvm_sequencer #({{ ip }}_{{ a.name }}_seq_item);

  `uvm_component_utils({{ ip }}_{{ a.name }}_sequencer)

  {{ ip }}_{{ a.name }}_cfg cfg;

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

endclass : {{ ip }}_{{ a.name }}_sequencer
