// -----------------------------------------------------------------------------
// {{ ip }}_{{ agent.name }}_agent_cfg — configuration of the '{{ agent.name }}' agent
// Generated by uvm-gen {{ gen_version }}.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_{{ agent.NAME }}_AGENT_CFG_SV
`define {{ IP }}_{{ agent.NAME }}_AGENT_CFG_SV

class {{ ip }}_{{ agent.name }}_agent_cfg extends uvm_object;

  // Active/passive switch. Vertical reuse: an SoC env flips this to
  // UVM_PASSIVE (RTL drives the bus) with NO source edits — driver and
  // sequencer are then never constructed, monitor/coverage stay live.
  uvm_active_passive_enum is_active = {{ agent.active_enum }};

  // Enable functional coverage collection for this interface
  bit coverage_enable = 1;

  // Virtual interface. Lives HERE, inside the config object — components
  // never fetch interfaces from the config_db by hierarchical path. The
  // standalone test (or the SoC env) populates it.
  virtual {{ ip }}_{{ agent.name }}_if vif;

  `uvm_object_utils_begin({{ ip }}_{{ agent.name }}_agent_cfg)
    `uvm_field_enum(uvm_active_passive_enum, is_active, UVM_ALL_ON)
    `uvm_field_int(coverage_enable, UVM_ALL_ON)
  `uvm_object_utils_end

  function new(string name = "{{ ip }}_{{ agent.name }}_agent_cfg");
    super.new(name);
  endfunction

endclass : {{ ip }}_{{ agent.name }}_agent_cfg

`endif // {{ IP }}_{{ agent.NAME }}_AGENT_CFG_SV
