// -----------------------------------------------------------------------------
// {{ ip }}_{{ agent.name }}_sequencer — sequencer of the '{{ agent.name }}' agent
// Generated by uvm-gen {{ gen_version }}.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_{{ agent.NAME }}_SEQUENCER_SV
`define {{ IP }}_{{ agent.NAME }}_SEQUENCER_SV

class {{ ip }}_{{ agent.name }}_sequencer extends uvm_sequencer #({{ ip }}_{{ agent.name }}_seq_item);

  {{ ip }}_{{ agent.name }}_agent_cfg cfg;

  `uvm_component_utils({{ ip }}_{{ agent.name }}_sequencer)

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

endclass : {{ ip }}_{{ agent.name }}_sequencer

`endif // {{ IP }}_{{ agent.NAME }}_SEQUENCER_SV
