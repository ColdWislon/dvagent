# Xcelium probing script for `make waves` (generated by uvm-gen {{ gen_version }})
database -open waves -shm -into waves.shm -default
probe -create {{ ip }}_tb_top -depth all -all -shm -database waves
run
exit
