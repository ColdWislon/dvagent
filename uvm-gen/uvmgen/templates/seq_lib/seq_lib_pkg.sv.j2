// -----------------------------------------------------------------------------
// {{ ip }}_seq_lib_pkg — env-level (virtual) sequence library
// Generated by uvm-gen {{ gen_version }}. Agent-level sequences live in the
// agent packages; add new virtual sequences here.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_SEQ_LIB_PKG_SV
`define {{ IP }}_SEQ_LIB_PKG_SV

package {{ ip }}_seq_lib_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

{% for a in agents %}
  import {{ ip }}_{{ a.name }}_agent_pkg::*;
{% endfor %}
  import {{ ip }}_env_pkg::*;

  `include "{{ ip }}_base_vseq.sv"
  `include "{{ ip }}_smoke_vseq.sv"

endpackage : {{ ip }}_seq_lib_pkg

`endif // {{ IP }}_SEQ_LIB_PKG_SV
