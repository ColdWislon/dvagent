// -----------------------------------------------------------------------------
// {{ ip }}_base_vseq — base virtual sequence
// Generated by uvm-gen {{ gen_version }}. Derive every env-level scenario
// from this class.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_BASE_VSEQ_SV
`define {{ IP }}_BASE_VSEQ_SV

class {{ ip }}_base_vseq extends uvm_sequence #(uvm_sequence_item);

  `uvm_object_utils({{ ip }}_base_vseq)
  `uvm_declare_p_sequencer({{ ip }}_virtual_sequencer)

  function new(string name = "{{ ip }}_base_vseq");
    super.new(name);
  endfunction

  virtual task pre_body();
    if (p_sequencer.cfg == null)
      `uvm_fatal(get_type_name(), "virtual sequencer has no {{ ip }}_env_cfg")
  endtask

endclass : {{ ip }}_base_vseq

`endif // {{ IP }}_BASE_VSEQ_SV
