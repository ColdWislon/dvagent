// -----------------------------------------------------------------------------
// {{ ip }}_{{ vip.name }}_vip_pkg — package of the '{{ vip.name }}' {{ vip.title }} VIP wrapper
// Generated by uvm-gen {{ gen_version }}.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_{{ vip.NAME }}_VIP_PKG_SV
`define {{ IP }}_{{ vip.NAME }}_VIP_PKG_SV

package {{ ip }}_{{ vip.name }}_vip_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

  // TODO(vip): import the Cadence {{ vip.title }} VIP UVM package, e.g.
  //   import {{ vip.example_pkg }}::*;
  // and activate the VIP compile lines in sim/vip_{{ vip.protocol }}.f
  // (anchored on $CDN_VIP_ROOT).

  `include "{{ ip }}_{{ vip.name }}_vip_cfg.sv"
  `include "{{ ip }}_{{ vip.name }}_vip_agent.sv"

endpackage : {{ ip }}_{{ vip.name }}_vip_pkg

`endif // {{ IP }}_{{ vip.NAME }}_VIP_PKG_SV
