// -----------------------------------------------------------------------------
// {{ ip }}_dut_stub — stand-in for '{{ dut.module }}'
// Generated by uvm-gen {{ gen_version }} so the bench elaborates before RTL
// is available. Compiled ONLY under +define+{{ IP }}_USE_DUT_STUB, which
// sim/Makefile adds automatically while the DUT filelist
// ({{ dut.rtl_filelist or "dut.rtl_filelist — set it in the config YAML" }}) does not resolve.
// -----------------------------------------------------------------------------
`ifdef {{ IP }}_USE_DUT_STUB

module {{ dut.module }};
  // Intentionally empty. The real RTL takes over as soon as the configured
  // dut.rtl_filelist exists — no edits needed here.
endmodule : {{ dut.module }}

`endif // {{ IP }}_USE_DUT_STUB
