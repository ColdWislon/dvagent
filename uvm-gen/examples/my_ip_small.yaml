# 'small' configuration of my_ip — everything shared comes from the base YAML
# (deep-merge; this file wins on conflicts).
extends: my_ip.yaml

config_name: small

params:
  DATA_W: 16
  FIFO_DEPTH: 8
