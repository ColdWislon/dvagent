# Example RTL filelist for my_ip (referenced by dut.rtl_filelist in the YAML).
# Paths in xrun -f files are resolved from the directory make runs in (sim/),
# so real projects typically use absolute paths or $VARIABLE prefixes here.
../../rtl/my_ip_top.sv
