# 'large' configuration of my_ip: deep-merges onto my_ip.yaml (this file wins).
extends: my_ip.yaml
config_name: large

params:
  DATA_WIDTH: 64
  NUM_CHANNELS: 4
  FIFO_DEPTH:
    value: 16
    style: defparam
