# Convenience wrapper (generated by uvm-gen {{ gen_version }}) — the real flow
# lives in sim/Makefile. Command-line variables (TEST=, CFG=, SEED=, ...)
# propagate automatically.
SIMDIR := $(dir $(lastword $(MAKEFILE_LIST)))sim

.PHONY: compile run waves regress matrix clean help
.DEFAULT_GOAL := help

compile run waves regress matrix clean help:
	@$(MAKE) -C $(SIMDIR) $@
