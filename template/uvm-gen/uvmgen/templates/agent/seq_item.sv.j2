// -----------------------------------------------------------------------------
// {{ ip }}_{{ agent.name }}_seq_item — transaction of the '{{ agent.name }}' agent
// Generated by uvm-gen {{ gen_version }}.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_{{ agent.NAME }}_SEQ_ITEM_SV
`define {{ IP }}_{{ agent.NAME }}_SEQ_ITEM_SV

class {{ ip }}_{{ agent.name }}_seq_item extends uvm_sequence_item;

  // TODO(protocol): replace with the real '{{ agent.name }}' transaction fields
  // (address, direction, burst, ... as the protocol requires).
  rand bit [31:0] data;

  // Example distribution — tune or remove with the real protocol.
  constraint c_data_dist {
    data dist { 32'h0 :/ 1, [32'h1 : 32'hFFFF_FFFE] :/ 8, 32'hFFFF_FFFF :/ 1 };
  }

  `uvm_object_utils_begin({{ ip }}_{{ agent.name }}_seq_item)
    `uvm_field_int(data, UVM_ALL_ON)
  `uvm_object_utils_end

  function new(string name = "{{ ip }}_{{ agent.name }}_seq_item");
    super.new(name);
  endfunction

  virtual function string convert2string();
    return $sformatf("data=0x%08h", data);
  endfunction

endclass : {{ ip }}_{{ agent.name }}_seq_item

`endif // {{ IP }}_{{ agent.NAME }}_SEQ_ITEM_SV
