// -----------------------------------------------------------------------------
// {{ ip }}_{{ agent.name }}_agent_pkg — package of the '{{ agent.name }}' agent
// Generated by uvm-gen {{ gen_version }}.
// Self-contained: no references to any testbench hierarchy — reusable as-is
// inside an SoC environment.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_{{ agent.NAME }}_AGENT_PKG_SV
`define {{ IP }}_{{ agent.NAME }}_AGENT_PKG_SV

package {{ ip }}_{{ agent.name }}_agent_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

  `include "{{ ip }}_{{ agent.name }}_seq_item.sv"
  `include "{{ ip }}_{{ agent.name }}_agent_cfg.sv"
  `include "{{ ip }}_{{ agent.name }}_sequencer.sv"
  `include "{{ ip }}_{{ agent.name }}_driver.sv"
  `include "{{ ip }}_{{ agent.name }}_monitor.sv"
  `include "{{ ip }}_{{ agent.name }}_agent.sv"
  `include "{{ ip }}_{{ agent.name }}_base_seq.sv"

endpackage : {{ ip }}_{{ agent.name }}_agent_pkg

`endif // {{ IP }}_{{ agent.NAME }}_AGENT_PKG_SV
