# {{ ip }} verification plan

Generated by uvm-gen {{ gen_version }} as a skeleton. Draft real items with
`/generate-vplan {{ ip }} <spec.pdf>` (text-dominant specs) or
`template/external-vplan-kit/` (table/diagram-heavy specs). Every test,
covergroup bin, and check ties back to a `VP-{{ IP }}-xxx` item here; the
`dv-test-writer` agent refuses work it cannot trace to a row in this table.

| ID | Spec ref | Requirement | Coverage mapping | Check mapping | Status |
|---|---|---|---|---|---|
| VP-{{ IP }}-001 | [spec §] | [one verifiable requirement] | [covergroup/coverpoint or `-`] | [check ID `SCBD_*`/`CHK_*` or `-`] | open |

Status values: `open` → `in-progress` → `covered` (stimulus + coverage bin) →
`checked` (a check confirms it) → `closed` (signed off).

<!--
Add one row per requirement. Keep IDs stable — tests reference them as
`// VP-{{ IP }}-xxx` tags, greppable for traceability. Coverage/Check columns
point at the actual covergroup bins and scoreboard/SVA check IDs that close
the item.
-->
