# {{ ip }} — per-IP context

Generated by uvm-gen {{ gen_version }} as a skeleton. This is the durable
context an engineer (or a Copilot DV agent) reads BEFORE working on {{ ip }}.
Fill the brackets in — a bracket-heavy file here is a first-week task, not a
finished one.

## What this block is
[One paragraph: what {{ ip }} does, where it sits, who owns it.]

## DUT
- Top module: `{{ dut.module }}`
- RTL filelist: `{{ dut.rtl_filelist or "TODO: set dut.rtl_filelist in cfg/*.yaml" }}`
- Revision policy: [tagged drops? git hash pinned in the session report? note it]

## Interfaces
{% for a in agents %}
- **{{ a.name }}** ({{ a.mode }}) — [protocol, width, direction, quirks]
{% endfor %}
{% for v in vips %}
- **{{ v.name }}** — Cadence {{ v.title }} VIP, role `{{ v.role }}` — [what it drives/checks]
{% endfor %}
{% if not agents and not vips %}
- [no interfaces configured yet]
{% endif %}

## Configurations
{% for p in params %}
- `{{ p.name }}` — [meaning, legal range, which configs exercise it]
{% endfor %}
{% if not params %}
- [no parameters — single configuration]
{% endif %}
Each configuration is one `cfg/*.yaml`; the param hash in every run's
`CFG_BANNER` / `verif_matrix.yaml` record identifies exactly what ran.

## Known quirks / gotchas
- [Things the files can't tell an agent: "reset needs N cycles", "field X is
  RES0", "the Y path has a known errata". Add here whenever you repeat
  yourself twice in a session.]

## Key references
- Spec: [doc + version]
- Verification plan: `docs/vplan.md`
- Methodology / Definition of Done: `template/docs/methodology/definition-of-done.md`
  (lives at the pack/workspace root, not inside this env; absent in a
  standalone env — use your team's block DoD there)
