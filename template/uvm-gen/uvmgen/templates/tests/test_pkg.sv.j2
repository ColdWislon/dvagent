// -----------------------------------------------------------------------------
// {{ ip }}_test_pkg — test library package
// Generated by uvm-gen {{ gen_version }}. Add new tests here and register
// them in sim/{{ vsif_name }} for the regression.
// -----------------------------------------------------------------------------
`ifndef {{ IP }}_TEST_PKG_SV
`define {{ IP }}_TEST_PKG_SV

package {{ ip }}_test_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

{% for a in agents %}
  import {{ ip }}_{{ a.name }}_agent_pkg::*;
{% endfor %}
{% for v in vips %}
  import {{ ip }}_{{ v.name }}_vip_pkg::*;
{% endfor %}
  import {{ ip }}_env_pkg::*;
  import {{ ip }}_seq_lib_pkg::*;

  `include "{{ ip }}_base_test.sv"
  `include "{{ ip }}_smoke_test.sv"

endpackage : {{ ip }}_test_pkg

`endif // {{ IP }}_TEST_PKG_SV
