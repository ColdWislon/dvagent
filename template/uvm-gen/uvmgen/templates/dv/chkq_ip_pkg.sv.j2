// ---------------------------------------------------------------------------
// {{ ip }}_chkq_pkg — {{ ip }}'s checker-qualification test package.
// Generated by uvm-gen {{ gen_version }}. Compiled only via the opt-in
// dv/tests/negative/chkq.f filelist; never part of the functional build.
// ---------------------------------------------------------------------------
`ifndef {{ IP }}_CHKQ_PKG_SV
`define {{ IP }}_CHKQ_PKG_SV

package {{ ip }}_chkq_pkg;

  import uvm_pkg::*;
  `include "uvm_macros.svh"

  import chkq_pkg::*;
{% for a in agents %}
  import {{ ip }}_{{ a.name }}_agent_pkg::*;
{% endfor %}
  import {{ ip }}_env_pkg::*;
  import {{ ip }}_seq_lib_pkg::*;
  import {{ ip }}_test_pkg::*;

  `include "chkq_paths.svh"
  `include "{{ ip }}_chkq_base_test.sv"
  `include "{{ ip }}_example_neg_test.sv"

  // TODO(chkq): `include one persistent negative test per qualified check ID
  // (dv-checker-writer Gate 4) and register it in dv/lists/chkq.list.

endpackage : {{ ip }}_chkq_pkg

`endif // {{ IP }}_CHKQ_PKG_SV
