// ---------------------------------------------------------------------------
// {{ ip }} chkq HDL-path registry — the ONE audit point for injection paths.
// Generated by uvm-gen {{ gen_version }}.
//
// Every DUT signal a negative test forces lives here as a package-scope
// localparam string named CHKQ_PATH_<CHECK_ID> — never as a literal in a
// test. When the RTL hierarchy is refactored, this file is the only place to
// fix; a stale path fails loudly as CHKQ_PATH at run time (not silently).
//
// Paths are absolute from the testbench top ({{ ip }}_tb_top). Included inside
// {{ ip }}_chkq_pkg, so these become package constants.
// ---------------------------------------------------------------------------
`ifndef {{ IP }}_CHKQ_PATHS_SVH
`define {{ IP }}_CHKQ_PATHS_SVH

// TODO(chkq): one localparam per qualified check, e.g.
//   localparam string CHKQ_PATH_SCBD_DATA_CMP =
//       "{{ ip }}_tb_top.dut.u_rd_path.rdata_q";
// then reference CHKQ_PATH_SCBD_DATA_CMP (never the literal) from the
// negative test's injector.force_for(...) call.

`endif // {{ IP }}_CHKQ_PATHS_SVH
