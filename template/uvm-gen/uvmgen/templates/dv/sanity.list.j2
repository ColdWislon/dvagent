# {{ ip }} sanity regression list (generated by uvm-gen {{ gen_version }}).
# One functional test class per line; blank lines and #-comments ignored.
# These run in the normal functional build (no +CHKQ_ENABLE), coverage as
# configured. Extend as tests land; keep the smoke test first.
{{ ip }}_smoke_test
