# {{ ip }} checker-qualification regression list (generated by uvm-gen {{ gen_version }}).
# One negative-test class per line; blank lines and #-comments ignored.
# Every entry runs with +CHKQ_ENABLE, -access +rwc, coverage OFF, and is
# EXCLUDED from functional/coverage merges. A CHKQ_BLIND failure here is a
# high-severity event: a checker went blind, so prior "clean" reports on that
# check may be unsound.
#
# dv-checker-writer Gate 4 appends one persistent test per qualified check ID.
{{ ip }}_example_neg_test
