# {{ ip }} coverage exclusion requests

Generated by uvm-gen {{ gen_version }}. Agents (and engineers) **append**
proposed exclusions here; a **human applies** them to the coverage refinement
files. An exclusion is a documented decision, never a silent waiver — every
row needs a justification tying it to a spec statement or a configuration that
cannot reach the bin.

| Date | Coverpoint / bin | Kind (A/B/C/D) | Justification (spec § or config) | Proposed by | Status |
|---|---|---|---|---|---|
| | | | | | |

<!--
Kinds: A = unreachable by construction, B = unreachable in this configuration
(name the param), C = legal-but-not-targeted (needs review), D = illegal/
don't-care. C/D require explicit human sign-off before application. Status:
proposed → approved → applied (in the IMC refinement file) / rejected.
-->
