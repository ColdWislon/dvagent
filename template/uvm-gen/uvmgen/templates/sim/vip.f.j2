# ==============================================================================
# Cadence {{ proto.title }} VIP compile filelist for {{ ip }}
# (generated by uvm-gen {{ gen_version }})
#
# $CDN_VIP_ROOT is THE single knob for the VIP installation — sim/Makefile
# exports it and refuses to run while it is unset and the active config
# requests VIPs. When your VIP install moves, this file needs one edit.
#
# TODO(site): activate the lines for your VIP release (paths and package
# names vary per release — see the {{ proto.title }} VIP release notes under
# $CDN_VIP_ROOT). Typical shape:
#
#   -incdir $CDN_VIP_ROOT/<path-to-{{ proto.key }}-uvm-sv-sources>
#   -f      $CDN_VIP_ROOT/<path-to-{{ proto.key }}-uvm-sv-compile.f>
#
# Then, in the wrapper package (vip/*_vip/), import the VIP package
# (e.g. {{ proto.example_pkg }}) and follow the TODO(vip) markers.
# ==============================================================================
